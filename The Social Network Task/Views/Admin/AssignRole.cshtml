@* @model AssignRoleViewModel

@{
    ViewData["Title"] = "Assign Role to User";
}

<div class="container mt-5">
    <h2 class="mb-4">Assign Role to User</h2>
    <form asp-action="AssignRole" method="post" class="form-inline">
        <div class="form-group mb-2">
            <label for="user" class="mr-2">Select User:</label>
            <select id="user" name="userId" class="form-control mr-4">
                @foreach (var user in Model.Users)
                {
                    <option value="@user.Id">@user.UserName</option>
                }
            </select>
        </div>
        <div class="form-group mb-2">
            <label for="role" class="mr-2">Select Role:</label>
            <select id="role" name="role" class="form-control mr-4">
                @foreach (var role in Model.Roles)
                {
                    <option value="@role.Name">@role.Name</option>
                }
            </select>
        </div>
        <button type="submit" class="btn btn-primary mb-2">Assign Role</button>
    </form>
</div> *@

            @model AssignRoleViewModel

@{
    ViewData["Title"] = "Assign Role to User";
}

<div class="container mt-5">
    <h2 class="text-center mb-4">Assign Role to User</h2>

    <div class="card shadow-sm">
        <div class="card-body">
            <form asp-action="AssignRole" method="post">
                <div class="form-group mb-3">
                    <label for="user" class="form-label">Select User:</label>
                    <select id="user" name="userId" class="form-control">
                        @foreach (var user in Model.Users)
                        {
                            <option value="@user.Id">@user.UserName</option>
                        }
                    </select>
                </div>

                <div class="form-group mb-3">
                    <label for="role" class="form-label">Select Role:</label>
                    <select id="role" name="role" class="form-control">
                        @foreach (var role in Model.Roles)
                        {
                            <option value="@role.Name">@role.Name</option>
                        }
                    </select>
                </div>

                <div class="text-center">
                    <button type="submit" class="btn btn-primary w-50">Assign Role</button>
                </div>
            </form>
        </div>
    </div>
</div>